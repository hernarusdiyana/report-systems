{
    "builds": [{
        "src": "report_features/wsgi.py",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.12.3"}
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "report_features/wsgi.py"
        }
    ]
}